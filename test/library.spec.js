/* global describe, it, before */
import chai from 'chai';
// import tcp, { udp } from '../lib/library.min.js';

chai.expect();

const expect = chai.expect;

describe('TCP tests', () => {
  describe('Null', () => {
    it('should return ', () => {
      expect(null).to.be.equal(null);
    });
  });
});

describe('UDP tests', () => {
  describe('Null', () => {
    it('should return ', () => {
      expect(null).to.be.equal(null);
    });
  });
});
